{{$NEXT}}

    - Rewrite the AI/LLM policy as AI_AND_LLM_POLICY.md, shipped as the one internal Markdown file a distribution gathers back by name.

    - Move AI-use notes to the commit, pull request, or ticket instead of a BOT username, and state that they exist to give maintainers a rough sense of how much incoming work is AI generated.

    - Give the AI/LLM policy two stated goals: maintainer understanding, and keeping the burden of externally submitted AI/LLM work manageable.

    - Clarify that barring bot accounts from tickets and pull requests restricts who takes part, not whether submitted code was AI-assisted.

    - State that the AI/LLM policy manages the burden outside contributions place on core maintainers rather than setting a standard the repository is held to.

    - Track the shared commit a project last synced with, checking each session for pending updates and leaving the decision to sync or skip with the user.

    - Bar bot accounts from pull requests and ticket discussion, allowing only a disclosed initial bug report before the thread is left to humans.

    - Let a project declare where the Agents checkout lives, and require agents to ask the user rather than clone into a location nobody chose.

    - Limit RULINGS.md to decisions that could be raised again, keeping routine calls nobody will question out of every future reader's context.

    - Define an optional project RULINGS.md recording owner decisions, read only when a decision is needed, with recorded rulings flagged for the owner rather than overturned.

    - State that the AI/LLM policy guides new contributions toward maintainer understanding, and that proven contributors may deviate when quality holds.

    - Require the agent fixing review findings to run the full test suite, while reviewers still assume the implementer left it green.

    - Make KISS the standing default while code is written, with simple-to-use human interfaces as the one exception.

    - Bound review loops with a frozen scope manifest, a cumulative growth halt, optional-hardening findings, explicit owner-decision triggers, and archive refs.

    - Halt review loops that produce oversized fixes or run nine unclean rounds, flagging the cause for the owner.

    - Bound review cycles to the work before them, with one fix commit per cycle and pre-existing problems tracked instead of fixed.

    - Require adopting distributions to ship one synchronized AI/LLM policy unless a project override replaces it.

    - Require brief, single-line Changes bullets in shared agent and review guidance.

    - Require same-model independent review cycles without redundant review-time suite runs, and define a bounded user-requested multi-model review that reports combined findings without automatic fixes.

    - Exempt documentation-only changes in the shared-guidance repository from mandatory independent review cycles.

    - Restrict documentation references by medium, with committed Markdown and comments pointing only to committed Markdown targets.

    - Keep work in the Agents repository on master unless the owner explicitly requests a worktree or integration shape.

    - Allow minor work on the primary branch and one-commit worktrees to fast-forward while preserving merge commits for multi-commit worktrees.

    - Require agents to flag speculative or disproportionate complexity for owner cost-benefit decisions.

    - Centralize brief documentation rules by audience, including narrow comment references to committed AI documents.

    - Keep auditor diagnostics self-contained instead of referring users to
      internal Markdown filenames.

    - Leave dependency classification, prerequisite tiers, and load behavior
      entirely to each project's own architecture and packaging policy.

    - Require projects to inventory their own non-Perl test suites without
      prescribing dependencies, runners, coverage thresholds, or CI.

    - State the repository's Perl 5 license explicitly.

    - Use Codex's documented user and repository skill-discovery paths and
      current explicit invocation forms while retaining optional symlinks.

    - Preserve CPAN Testers, test-command, and related-repository knowledge
      when replacing harness entry points, with a shared report procedure.

    - Verify adopted projects' Perl-floor declarations against packaging and
      shipped module pragmas without coupling them to signature policy.

    - Add a parameterized test-layout auditor for projects that select the
      category-and-origin profile, without imposing it on other layouts.

    - Keep dependency classification and continuous-integration policy local
      to each project while making the optional packaging profile internally
      consistent and documenting the manual release boundary.

    - Give each decision, test, and pre-review procedure one authoritative
      home and include staged, unstaged, and untracked files in handoff gates.

    - Move database-specific test operations into an optional guide and align
      test commands, timeouts, author-test state, and debris safety wording.

    - Separate Perl compatibility from signature policy, standardize five
      project declarations, and limit terminology rules to maintained code and
      user-facing documentation.

    - Define Agents adoption as optional, give project documents unconditional
      priority, and load only entry-point references relevant to the task.

    - Cover quote-like operators, heredocs, POD, and data trailers in scanner
      tests so source-like text is not mistaken for executable code.

    - Separate Dist::Zilla packaging checks from optional Agents project
      wiring, with stable diagnostic codes for both audits.

    - Give database-debris cleanup a one-hour safety age and refuse to proceed
      when live-watcher enumeration fails.

    - Make the test wrapper force author-test state from its command line and
      derive default timeouts from serial versus concurrent execution.

    - Stop treating relative project paths as possible high-entropy secrets.

    - Count executable lines rather than comments and blank lines when
      enforcing the subroutine-size limit.

    - Audit private inlined HashBase classes and detect read-only attributes
      on single-line and closing-line imports.

    - Limit terminology checks to maintained code and identified user
      documentation while excluding internal history and worktrees.

    - Make optional installation preserve user-owned skill symlinks and work
      from a fresh home directory.
